[
    {
        "name": "list_tables",
        "description": "Devuelve todas las tablas del esquema actual.",
        "parameters": {
            "type": "object",
            "properties": {},
            "required": []
        }
    },
    {
        "name": "get_columns",
        "description": "Devuelve las columnas de una tabla, incluyendo tipo, nullability y default.",
        "parameters": {
            "type": "object",
            "properties": {
                "table": { "type": "string" }
            },
            "required": ["table"]
        }
    },
    {
        "name": "get_primary_keys",
        "description": "Devuelve claves primarias de una tabla.",
        "parameters": {
            "type": "object",
            "properties": {
                "table": { "type": "string" }
            },
            "required": ["table"]
        }
    },
    {
        "name": "get_foreign_keys",
        "description": "Devuelve claves foráneas de una tabla.",
        "parameters": {
            "type": "object",
            "properties": {
                "table": { "type": "string" }
            },
            "required": ["table"]
        }
    },
    {
        "name": "describe_table",
        "description": "Devuelve la estructura completa de una tabla: columnas, PK, FK.",
        "parameters": {
            "type": "object",
            "properties": {
                "table": { "type": "string" }
            },
            "required": ["table"]
        }
    },
    {
        "name": "execute_query",
        "description": "Ejecuta una consulta SQL y devuelve el resultado.",
        "parameters": {
            "type": "object",
            "properties": {
                "sql": { "type": "string" }
            },
            "required": ["sql"]
        }
    }
]
